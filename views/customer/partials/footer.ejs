    <!-- Footer -->
    <footer class="bg-dark text-white mt-5">
        <div class="container py-5">
            <!-- Trust Indicators -->
            <div class="row mb-4 pb-4 border-bottom border-secondary">
                <div class="col-md-12 text-center">
                    <div class="d-flex flex-wrap justify-content-center gap-3">
                        <div class="security-indicator">
                            <i class="bi bi-shield-lock-fill" style="color: #28a745;"></i>
                            <span>Secure & Safe</span>
                        </div>
                        <div class="security-indicator">
                            <i class="bi bi-award-fill" style="color: #ffc107;"></i>
                            <span>Quality Guaranteed</span>
                        </div>
                        <div class="security-indicator">
                            <i class="bi bi-people-fill" style="color: #17a2b8;"></i>
                            <span>Trusted by Thousands</span>
                        </div>
                        <div class="security-indicator">
                            <i class="bi bi-star-fill" style="color: #ffd700;"></i>
                            <span>5-Star Service</span>
                        </div>
                    </div>
                </div>
            </div>
            
            <div class="row">
                <div class="col-md-4 mb-4">
                    <h5><i class="bi bi-lightning-charge-fill" style="color: #F7A400;"></i> Tushar Electronics</h5>
                    <p>Your trusted source for quality electronics. We offer the best in TVs, ACs, Washing Machines, and more.</p>
                    <div class="mt-3">
                        <p class="mb-2 phone-number text-white">
                            <i class="bi bi-telephone-fill"></i> 
                            <a href="tel:+919171310766" class="text-white text-decoration-none">+91 9171310766</a>
                        </p>
                        <p class="mb-2">
                            <i class="bi bi-envelope"></i> 
                            <a href="mailto:tusharelectronics8439@gmail.com" class="text-white-50 text-decoration-none">tusharelectronics8439@gmail.com</a>
                        </p>
                    </div>
                    <div class="mt-3">
                        <p class="mb-2"><strong>Why Choose Us?</strong></p>
                        <ul class="list-unstyled">
                            <li><i class="bi bi-check-circle text-success me-2"></i> Authentic Products</li>
                            <li><i class="bi bi-check-circle text-success me-2"></i> Best Prices</li>
                            <li><i class="bi bi-check-circle text-success me-2"></i> Expert Support</li>
                            <li><i class="bi bi-check-circle text-success me-2"></i> Fast Delivery</li>
                        </ul>
                    </div>
                </div>
                <div class="col-md-2 mb-4">
                    <h6>Quick Links</h6>
                    <ul class="list-unstyled">
                        <li><a href="/about" class="text-white-50 text-decoration-none">About Us</a></li>
                        <li><a href="/contact" class="text-white-50 text-decoration-none">Contact</a></li>
                        <li><a href="/faq" class="text-white-50 text-decoration-none">FAQ</a></li>
                        <li><a href="/blog" class="text-white-50 text-decoration-none">Blog</a></li>
                    </ul>
                </div>
                <div class="col-md-2 mb-4">
                    <h6>Policies</h6>
                    <ul class="list-unstyled">
                        <li><a href="/privacy-policy" class="text-white-50 text-decoration-none">Privacy Policy</a></li>
                        <li><a href="/terms-conditions" class="text-white-50 text-decoration-none">Terms & Conditions</a></li>
                        <li><a href="/shipping-policy" class="text-white-50 text-decoration-none">Shipping Policy</a></li>
                        <li><a href="/return-refund-policy" class="text-white-50 text-decoration-none">Return Policy</a></li>
                    </ul>
                </div>
                <div class="col-md-4 mb-4">
                    <h6>Newsletter</h6>
                    <p class="text-white-50">Subscribe to get updates on new products and offers.</p>
                    <form id="newsletterForm" class="d-flex">
                        <input type="email" class="form-control form-control-sm" placeholder="Your email" required>
                        <button type="submit" class="btn btn-primary btn-sm">Subscribe</button>
                    </form>
                </div>
            </div>
            <hr class="bg-white">
            <div class="text-center">
                <p class="mb-0">&copy; <%= new Date().getFullYear() %> Tushar Electronics. All rights reserved.</p>
            </div>
        </div>
    </footer>

    <!-- Floating Call Button -->
    <a href="tel:+919171310766" class="call-float" title="Call Now">
        <i class="bi bi-telephone-fill"></i>
    </a>

    <!-- WhatsApp Button -->
    <a href="https://wa.me/919171310766" class="whatsapp-float" target="_blank" title="Chat on WhatsApp">
        <i class="bi bi-whatsapp"></i>
    </a>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
    <script>
    // Search Suggestions Functionality
    document.addEventListener('DOMContentLoaded', function() {
        const searchInput = document.getElementById('searchInput');
        const suggestionsDiv = document.getElementById('searchSuggestions');
        let currentFocus = -1;
        let suggestionsData = { categories: [], products: [] };
        let debounceTimer;

        if (!searchInput || !suggestionsDiv) {
            console.log('Search elements not found');
            return;
        }

        // Fetch suggestions from API
        async function fetchSuggestions(query) {
            if (query.length < 2) {
                suggestionsDiv.classList.remove('show');
                return;
            }

            try {
                const response = await fetch(`/api/search-suggestions?q=${encodeURIComponent(query)}`);
                const data = await response.json();
                suggestionsData = data;
                displaySuggestions(data);
            } catch (error) {
                console.error('Error fetching suggestions:', error);
                suggestionsDiv.classList.remove('show');
            }
        }

        // Display suggestions
        function displaySuggestions(data) {
            if ((!data.categories || data.categories.length === 0) && 
                (!data.products || data.products.length === 0)) {
                suggestionsDiv.classList.remove('show');
                return;
            }

            let html = '';

            // Categories section
            if (data.categories && data.categories.length > 0) {
                html += '<div class="suggestion-section"><i class="bi bi-grid-3x3-gap-fill me-2" style="color: #4A90E2;"></i>Categories</div>';
                data.categories.forEach(category => {
                    html += `
                        <div class="suggestion-item" data-type="category" data-url="/category/${category.slug}">
                            <i class="bi bi-tag-fill suggestion-icon" style="color: #F7A400;"></i>
                            <div class="suggestion-content">
                                <div class="suggestion-title">${escapeHtml(category.name)}</div>
                            </div>
                        </div>
                    `;
                });
            }

            // Products section
            if (data.products && data.products.length > 0) {
                html += '<div class="suggestion-section"><i class="bi bi-box-seam-fill me-2" style="color: #E74C3C;"></i>Products</div>';
                data.products.forEach(product => {
                    const price = product.price ? `₹${product.price.toLocaleString()}` : '';
                    const categoryName = product.category_id ? product.category_id.name : '';
                    html += `
                        <div class="suggestion-item" data-type="product" data-url="/product/${product.slug}">
                            <i class="bi bi-box-seam suggestion-icon" style="color: #28a745;"></i>
                            <div class="suggestion-content">
                                <div class="suggestion-title">${escapeHtml(product.name)}</div>
                                <div class="suggestion-subtitle">
                                    ${categoryName ? `<span class="suggestion-category">${escapeHtml(categoryName)}</span> ` : ''}${price}
                                </div>
                            </div>
                        </div>
                    `;
                });
            }

            suggestionsDiv.innerHTML = html;
            suggestionsDiv.classList.add('show');
            currentFocus = -1;

            // Add click handlers
            const items = suggestionsDiv.querySelectorAll('.suggestion-item');
            items.forEach((item, index) => {
                item.addEventListener('click', function() {
                    const url = this.getAttribute('data-url');
                    if (url) {
                        window.location.href = url;
                    }
                });

                item.addEventListener('mouseenter', function() {
                    items.forEach(i => i.classList.remove('active'));
                    this.classList.add('active');
                    currentFocus = index;
                });
            });
        }

        // Escape HTML to prevent XSS
        function escapeHtml(text) {
            const div = document.createElement('div');
            div.textContent = text;
            return div.innerHTML;
        }

        // Handle input
        searchInput.addEventListener('input', function(e) {
            const query = e.target.value.trim();
            clearTimeout(debounceTimer);
            debounceTimer = setTimeout(() => fetchSuggestions(query), 300);
        });

        // Handle keyboard navigation
        searchInput.addEventListener('keydown', function(e) {
            const items = suggestionsDiv.querySelectorAll('.suggestion-item');
            
            if (e.key === 'ArrowDown') {
                e.preventDefault();
                currentFocus++;
                if (currentFocus >= items.length) currentFocus = 0;
                updateActiveItem(items);
            } else if (e.key === 'ArrowUp') {
                e.preventDefault();
                currentFocus--;
                if (currentFocus < 0) currentFocus = items.length - 1;
                updateActiveItem(items);
            } else if (e.key === 'Enter') {
                e.preventDefault();
                if (currentFocus >= 0 && items[currentFocus]) {
                    const url = items[currentFocus].getAttribute('data-url');
                    if (url) {
                        window.location.href = url;
                    }
                } else {
                    searchInput.form.submit();
                }
            } else if (e.key === 'Escape') {
                suggestionsDiv.classList.remove('show');
                currentFocus = -1;
            }
        });

        function updateActiveItem(items) {
            items.forEach(item => item.classList.remove('active'));
            if (items[currentFocus]) {
                items[currentFocus].classList.add('active');
                items[currentFocus].scrollIntoView({ block: 'nearest', behavior: 'smooth' });
            }
        }

        // Close suggestions when clicking outside
        document.addEventListener('click', function(e) {
            if (!searchInput.contains(e.target) && !suggestionsDiv.contains(e.target)) {
                suggestionsDiv.classList.remove('show');
            }
        });
    });
    </script>
    <script>
    // Final carousel initialization check - ensure it's working
    if (typeof bootstrap !== 'undefined' && bootstrap.Carousel) {
        const carouselElement = document.querySelector('#heroCarousel');
        if (carouselElement) {
            const items = carouselElement.querySelectorAll('.carousel-item');
            if (items.length > 0) {
                let carousel = bootstrap.Carousel.getInstance(carouselElement);
                if (!carousel) {
                    try {
                        carousel = new bootstrap.Carousel(carouselElement, {
                            interval: 2000,
                            ride: false,
                            wrap: true,
                            pause: 'hover',
                            keyboard: true
                        });
                        carousel.cycle();
                    } catch (error) {
                        console.error('Error initializing carousel in footer:', error);
                    }
                } else if (!carousel._interval) {
                    carousel.cycle();
                }
            }
        }
    }
    </script>
</body>
</html>

